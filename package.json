{
  "name": "nov_proj",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/miao17game/NOV_PROJ.git",
  "author": "bigmogician <bigmogician@outlook.com>",
  "license": "Apache License 2.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "cd server && yarn start:dev",
    "start:prod": "cd server && yarn start:prod",
    "dev": "cd client && yarn start",
    "build:server": "cd server && yarn build",
    "build:client": "cd client && yarn build",
    "build": "yarn build:client && yarn build:server",
    "postbuild": "rimraf server/build && npx copy client/dist/client/** server/build"
  },
  "dependencies": {},
  "devDependencies": {}
}
