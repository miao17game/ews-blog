{
  "name": "editable-web-system",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/amoebajs/portal.git",
  "contributors": [
    "bigmogician <bigmogician@outlook.com>"
  ],
  "license": "Apache License 2.0",
  "private": true,
  "scripts": {
    "install:client": "cd client && yarn install",
    "install:server": "cd server && yarn install",
    "install": "yarn install:client && yarn install:server",
    "start": "cd server && yarn start:dev",
    "start:prod": "cd server && yarn start:prod",
    "dev": "cd client && yarn start",
    "build:server": "cd server && yarn build",
    "build:client": "cd client && yarn build",
    "build": "yarn build:client && yarn build:server",
    "postbuild": "rimraf server/build && copy client/dist/**/*.* server/build"
  },
  "dependencies": {},
  "devDependencies": {
    "copy": "^0.3.2",
    "rimraf": "^3.0.0"
  }
}
